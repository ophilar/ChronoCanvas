# Required server listen port. Cloud Run injects this in production.
PORT=3000

# Optional Gemini canvas-boundary detection. Configure both values to enable it.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.7-flash
